<% flash.each do |key, msg| %>
  <div class="alert alert-<%= key %>">
    <a class="close" data-dismiss="alert">&#215;</a>
    <%= content_tag :div, msg, :id => "flash_#{key}" if msg.is_a?(String) %>
  </div>
<% end %>
